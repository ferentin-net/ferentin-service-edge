apiVersion: v1
kind: Secret
metadata:
  name: service-edge-secrets
  namespace: ferentin-service-edge
  labels:
    app.kubernetes.io/name: service-edge
    app.kubernetes.io/component: config
type: Opaque
stringData:
  # Required: Enrollment token from admin console (for first-time setup)
  # After enrollment, edge identity is stored in the certs PVC
  enrollment-token: "your-enrollment-token-here"
